XLayerAPI is a Spring Boot application that takes transactions and makes them available to the XLayer Integration Platform.

Transactions that are received are written to either a database or to Kafka.